class Solution {
    public int[] runningSum(int[] nums) {

        int sum = 0;
        int i;
        int[] myList = new int[nums.length];
        for(i=0;i<nums.length;i++){
            sum = sum + nums[i];
            myList[i] = sum;
        }
        return myList;
        
    }
}
